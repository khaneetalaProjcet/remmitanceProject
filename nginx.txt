

server {
    listen 80;
    server_name asugold.com; # Main domain
    return 301 https://$host$request_uri; # Redirect HTTP to HTTPS
}

server {
    listen 443 ssl;
    server_name asugold.com;

    ssl_certificate /etc/nginx/ssl/asugold.com-191hYQtoqiBRWi__BnCj81NWvgveWqQplkD8ldsM1Io-cert.pem 
    ssl_certificate_key /etc/nginx/ssl/asugold.com-191hYQtoqiBRWi__BnCj81NWvgveWqQplkD8ldsM1Io-cert.pem 
   
     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    location / {
        root /home/site/site/build; # Path to your React build folder
        try_files $uri $uri/ /index.html; # Serve index.html for single-page app routing
    }
}

server {
    listen 80;
    server_name panel.asugold.com; # Subdomain
    return 301 https://$host$request_uri; # Redirect HTTP to HTTPS
}

server {
    listen 443 ssl;
    server_name panel.asugold.com;

    ssl_certificate /etc/nginx/ssl/asugold.com-191hYQtoqiBRWi__BnCj81NWvgveWqQplkD8ldsM1Io-cert.pem 
    ssl_certificate_key /etc/nginx/ssl/asugold.com-191hYQtoqiBRWi__BnCj81NWvgveWqQplkD8ldsM1Io-cert.pem 


    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    location / {
        root /home/panel/panel/build; # Path to your subdomain React build folder
        try_files $uri $uri/ /index.html; # Serve index.html for single-page app routing
    }
}
